const express = require('express');
const router = express.Router();

// Placeholder for room routes
router.get('/', (req, res) => {
  res.json({ message: 'Room routes - Coming soon' });
});

module.exports = router;
