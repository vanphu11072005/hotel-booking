const express = require('express');
const router = express.Router();

// Placeholder for promotion routes
router.get('/', (req, res) => {
  res.json({ message: 'Promotion routes - Coming soon' });
});

module.exports = router;
