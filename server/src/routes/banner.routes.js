const express = require('express');
const router = express.Router();

// Placeholder for banner routes
router.get('/', (req, res) => {
  res.json({ message: 'Banner routes - Coming soon' });
});

module.exports = router;
